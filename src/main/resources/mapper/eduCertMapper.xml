<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.EduCertMapper">

    <!-- 내 수료증 목록 조회 (과정명 JOIN) -->
    <select id="selectByUserId" parameterType="Long" resultType="EduCertResponseDTO">
        SELECT TEC.ID          AS id,
               TEC.EDU_CERT_CREATE_AT AS eduCertCreateAt,
               TEC.EDU_ID     AS eduId,
               TEC.USER_ID    AS userId,
               TE.EDU_TITLE   AS eduTitle
        FROM TBL_EDU_CERT TEC
                 JOIN TBL_EDU TE ON TEC.EDU_ID = TE.ID
        WHERE TEC.USER_ID = #{userId}
        ORDER BY TEC.EDU_CERT_CREATE_AT DESC
    </select>

</mapper>
