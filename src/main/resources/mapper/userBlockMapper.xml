<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.UserBlockMapper">
    <insert id="insert" parameterType="UserBlockVO">
        INSERT INTO TBL_USER_BLOCK (ID, BLOCKER_ID, BLOCKING_ID)
        VALUES (SEQ_USER_BLOCK.NEXTVAL, #{blockerId}, #{blockingId})
    </insert>
</mapper>