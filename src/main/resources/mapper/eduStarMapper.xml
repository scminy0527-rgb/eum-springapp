<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.EduStartMapper">

    <!-- 사용자와 학습 번호로 시작 기록 조회 -->
    <select id="selectByUserIdAndEduId" resultType="EduStartResponseDTO">
        SELECT ID, EDU_START_AT, USER_ID, EDU_ID
        FROM TBL_EDU_START
        WHERE USER_ID = #{userId}
          AND EDU_ID = #{eduId}
    </select>

    <!-- 학습 시작 기록 등록 -->
    <insert id="insert" parameterType="EduStartVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT SEQ_EDU_START.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_EDU_START(ID, EDU_START_AT, USER_ID, EDU_ID)
        VALUES(#{id}, CURRENT_TIMESTAMP, #{userId}, #{eduId})
    </insert>

</mapper>