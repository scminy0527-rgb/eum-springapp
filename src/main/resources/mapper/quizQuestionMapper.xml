<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.QuizQuestionMapper">

    <!-- 퀴즈별 문제 목록 조회 -->
    <select id="selectByQuizId" parameterType="Long" resultType="QuizQuestionResponseDTO">
        SELECT TQQ.ID, TQQ.QUIZ_QUESTION_TYPE, TQQ.QUIZ_QUESTION_DETAIL, TQQ.QUIZ_QUESTION_CREATE_AT, TQQ.WORDS_ID
        FROM TBL_QUIZ_EDU TQE
        JOIN TBL_QUIZ_QUESTION TQQ
            ON TQE.QUIZ_QUESTION_ID = TQQ.ID
        WHERE TQE.QUIZ_ID = #{quizId}
        ORDER BY TQE.ID ASC
    </select>

    <!-- 문제 상세 조회 -->
    <select id="select" parameterType="Long" resultType="QuizQuestionResponseDTO">
        SELECT ID, QUIZ_QUESTION_TYPE, QUIZ_QUESTION_DETAIL, QUIZ_QUESTION_CREATE_AT, WORDS_ID
        FROM TBL_QUIZ_QUESTION
        WHERE ID = #{id}
    </select>

</mapper>