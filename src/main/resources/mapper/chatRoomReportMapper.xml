<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.ChatRoomReportMapper">
    <insert id="insert" parameterType="ChatRoomReportVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT SEQ_CHAT_ROOM_REPORT.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_CHAT_ROOM_REPORT (ID, CHAT_ROOM_REPORT_TITLE, CHAT_ROOM_REPORT_DETAIL, USER_ID, CHAT_ROOM_ID)
        VALUES (#{id}, #{chatRoomReportTitle}, #{chatRoomReportDetail}, #{userId}, #{chatRoomId})
    </insert>
</mapper>