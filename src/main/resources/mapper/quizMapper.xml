<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.QuizMapper">

    <!-- 퀴즈 목록 조회 -->
    <select id="selectAll" resultType="QuizResponseDTO">
        SELECT ID, QUIZ_TITLE, QUIZ_DETAIL, QUIZ_CATEGORY, QUIZ_THUMBNAIL, QUIZ_CREATE_AT
        FROM TBL_QUIZ
        ORDER BY ID DESC
    </select>

    <!-- 퀴즈 상세 조회 -->
    <select id="select" parameterType="Long" resultType="QuizResponseDTO">
        SELECT ID, QUIZ_TITLE, QUIZ_DETAIL, QUIZ_CATEGORY, QUIZ_THUMBNAIL, QUIZ_CREATE_AT
        FROM TBL_QUIZ
        WHERE ID = #{id}
    </select>

</mapper>